const PageHeader = ({ label }) => {

    return (
        <header>
            <div className="PageHeader">
                <h1>{label}</h1>
            </div>
        </header >
    )
}

export default PageHeader